# Инструкция по развертыванию NewCSSLearn

Приложение поддерживает два режима работы:
- **Локальная разработка** через Docker
- **Production развертывание** на хостинге Hoster.kz

---

## Локальная разработка через Docker

### Требования
- Docker и Docker Compose
- Git

### Запуск локальной среды
1. Клонируйте репозиторий
2. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
3. Запустите Docker Compose:
   ```bash
   docker-compose up -d
   ```
4. Приложение будет доступно по адресу: http://localhost:8080
5. PhpMyAdmin доступен по адресу: http://localhost:8081

### Остановка
```bash
docker-compose down
```

---

## Production развертывание на Hoster.kz

### Требования к хостингу
- PHP версии 8.2 или выше
- MySQL/MariaDB база данных
- Apache с поддержкой mod_rewrite
- Доступ к файловой системе для загрузки файлов

## Подготовка базы данных

1. Убедитесь, что база данных создана с именем `p-351366_new-css-learn`
2. Импортируйте структуру таблиц из файла `db-init.sql`
3. При необходимости импортируйте тестовые данные из `db-test-data.sql`

## Загрузка файлов на хостинг

1. Загрузите все файлы проекта в корневую директорию сайта
2. Установите права доступа:
   - Директории: 755
   - PHP файлы: 644
   - Файл `.htaccess`: 644

3. Убедитесь, что директория `uploads/lessons/` существует и имеет права на запись (755)

## Конфигурация

### Настройка базы данных
Откройте файл `config.php` и измените параметры подключения:
```php
define('DB_HOST', 'localhost'); // или адрес сервера БД
define('DB_NAME', 'p-351366_new-css-learn');
define('DB_USER', 'your_db_user'); // замените на реального пользователя
define('DB_PASS', 'your_db_password'); // замените на реальный пароль
```

### Настройка URL
Замените `https://bod-hub.kz` на ваш домен в `config.php`:
```php
define('APP_URL', 'https://your-domain.com');
```

### Настройка часового пояса
Текущая настройка: `Asia/Almaty`. При необходимости измените на ваш часовой пояс.

### Переключение между окружениями
- **Production**: по умолчанию, ничего не нужно менять
- **Development**: раскомментируйте строку `define('ENVIRONMENT', 'development');` в `config.php`

## Проверка работы

1. Откройте сайт в браузере - должна отобразиться главная страница с разделами
2. Проверьте админ-панель по адресу `/bod`
   - Логин: `bodryakov.web`
   - Пароль: `Anna-140275`

## Структура URL

- Главная страница: `/`
- Админ-панель: `/bod`
- Разделы: `/1-css-basics`, `/2-html-fundamentals` и т.д.
- Уроки: `/1-css-basics/1-introduction-to-css` и т.д.

## Важные файлы

- `.htaccess` - настройка URL rewriting и безопасность
- `config.php` - конфигурация приложения
- `Database.php` - класс работы с базой данных
- `uploads/lessons/` - директория для загрузки изображений

## Безопасность

- Файлы конфигурации защищены от прямого доступа
- Включена защита от XSS атак
- Отключен просмотр каталогов
- Настроено кэширование статических файлов

## Решение проблем

### 500 ошибка
1. Проверьте права доступа к файлам и директориям
2. Проверьте наличие `.htaccess` и его содержимое
3. Проверьте логи ошибок в панели управления хостингом

### Ошибка подключения к БД
1. Проверьте параметры подключения в `config.php`
2. Убедитесь, что база данных создана и доступна
3. Проверьте права пользователя БД

### Не работает URL rewriting
1. Убедитесь, что модуль mod_rewrite включен
2. Проверьте директиву `AllowOverride All` в конфигурации Apache

## Поддержка

При возникновении проблем:
1. Проверьте логи ошибок PHP
2. Проверьте логи ошибок Apache
3. Убедитесь, что все требования к хостингу выполнены
